#include <xmc_gpio.h>

#include <stdlib.h>
uint16_t bla;
uint32_t bla2 = 0xFEFE;

int main(void) {
uint8_t fooElements = 3, barElements = 5;
uint8_t bazElements = 5;
long *foo = calloc(fooElements , sizeof(long));
long *bar = calloc(barElements , sizeof(long));
long *baz = calloc(bazElements , sizeof(long));
}